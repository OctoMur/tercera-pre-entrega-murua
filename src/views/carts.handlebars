<h1>Carrito de Compras</h1>
{{#each products}}
    <div>
        <p>Producto: {{this.product.title}}</p>
        <p>Cantidad: {{this.quantity}}</p>
    </div>
{{/each}}

<p>Total: ${{this.totalPurchase}}</p>
<br>
<span>cart: {{cartId}}</span>
